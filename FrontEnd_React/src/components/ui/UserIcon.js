import React from "react";

const UserIcon = ({ className }) => {
    return (
        <img
        src="/user (1).png"
        className={className}
        alt="User Profile page"
        />
    );
}

export default UserIcon;